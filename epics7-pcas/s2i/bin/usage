#!/bin/bash -e
cat <<EOF
This is the jalalmostafa/epics-pcas S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build <source code path/URL> jalalmostafa/epics-pcas:7.0.4 <application image>

You can then run the resulting image via:
docker run <application image>

On Kubernetes, use: oc new-app jalalmostafa/epics-pcas:7.0.4~<git repo>

EOF
